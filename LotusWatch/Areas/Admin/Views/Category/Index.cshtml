
    @*<style>

        #tblCategoryData th,
        #tblCategoryData td {
        white-space: nowrap; /* Prevent line breaks within cells */
        overflow: hidden; /* Hide overflowing content */
        text-overflow: ellipsis; /* Add ellipsis (...) for truncated text */
        }

        #tblCategoryData{
            width: 100%;
        }

        #tblCategoryData td {
            max-width: 150px; /* Maximum width of the table cells */
        }
    </style>*@
	<div class="container p-3">
        <div class="row pt-4">
            <div class="col-6">
                <h2 class="text-dark">Category Management</h2>
            </div>
            <div class="col-6  text-end">
                <a asp-controller="Category" asp-action="Create" class="btn btn-outline-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Create New Category
                </a>
            </div>
            <hr />
        </div>
        <br /><br />
        <table id="tblCategoryData" class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Description</th>
                    <th></th>
                </tr>
            </thead>
        </table>
	</div>
@section Scripts {
    <script src="~/js/category.js"></script>
}